import { Testimonial } from "./schema";

/**
 * Zero seeded testimonials per AGENTS.md §4 and Phase I rules:
 * "Do not invent testimonials. Ship with zero seeded entries and a designed empty state."
 */
export const staticTestimonials: Testimonial[] = [];
